# 8 Histogram hover effect.

- Let's correlate buckets in the histogram with rows in the table. We should be able to indicate what are we looking at. For that to be usable, we need to highlight in the chart and the rows with the same color, which rows are affected in the bucket that is being highlighted.
